---
name: market-quadrant-map
description: "使用者要做特定產品類別的市場調查／競品分析，想把多個品牌的產品依「價格 x 風格」畫成象限圖（可點擊連到Amazon或官網銷售頁）時使用。觸發詞：市場調查、競品分析、象限圖、品牌比較圖、產品定位圖、market map、quadrant chart。"
---

# 市場象限圖（Market Quadrant Map）

協助使用者針對某一個產品類別，蒐集市面上多個品牌的代表性產品，畫成一張「Y軸=金額、X軸=風格」的象限圖，背景依入門/中階/高階分層，色塊可點擊連到該產品的實際銷售頁（Amazon優先，其次品牌官網）。最終以互動網頁為主要交付，圖片檔、資料表 Markdown、Figma Make 重現規格 Markdown 為選配匯出。

本 skill 附一份實際跑過完整流程的參考成品：`reference/quadrant-map-template.html`（PSU／電源供應器範例，18品牌28款產品）與對應資料 `reference/example-products.json`。新類別可以直接複製這份檔案，依裡面標示的 `EDIT-PER-CATEGORY` 註解逐段修改，不用從零手刻。

## 使用者已確認的設計決策（照這個做，不用每次都重新問）

1. **X軸「風格」光譜**：由 Claude 先根據該產品類別，提出一組建議的風格光譜兩端（例如：極簡 vs 復古、科技感 vs 溫潤感、實用本位 vs 電競化），並讓使用者確認或調整後才進入研究階段。不要跳過確認直接開始蒐集資料。
2. **入門/中階/高階分層門檻**：依 Claude 對該產品類別的市場常識判斷合理的價格帶門檻（不是用這次蒐集到的數字動態三等分），並在最終輸出中清楚註明所用門檻與判斷依據，方便使用者檢視、日後手動調整。
3. **輸出格式**：預設只做「互動網頁」（色塊可點擊跳轉銷售頁）。PNG圖片、資料表 Markdown、Figma Make 重現規格 Markdown 只在使用者要求時才額外做，不主動做——但一旦使用者要求任一種匯出，優先做成頁面上的下載按鈕（見下方「匯出按鈕」一節），而不是每次都用 SendUserFile 手動產一次性檔案。
4. **資料與連結來源**：一定要用網頁搜尋／瀏覽工具實際查詢 Amazon 與品牌官網，取得真實售價、產品描述、真實銷售連結，不能編造。銷售連結務必是該產品的「直接商品頁」，不能是搜尋結果頁（例如 `amazon.com/s?k=...`）——這是容易犯的錯，發布前要抽查。

## 工作流程

### 1. 啟動時用 AskUserQuestion 收集需求

必問：
- 產品類別/名稱是什麼（例如：藍牙耳機、電動牙刷、機械鍵盤）
- 要查找幾個品牌（10-20個，預設15；若使用者已在對話中指定數量或品牌清單就不用再問）
- 是否有指定必查的品牌清單（開放文字，允許使用者列出一定要包含的品牌，其餘由 Claude 補足到指定數量）
- 每個品牌要放幾款代表性產品（預設1款主力產品；使用者可要求同品牌放多款代表不同定位的產品）

可選：若使用者一開始就提到想要圖片檔、Markdown、或要在 Figma Make 重現，順便記下，不用另外問。

### 2. 定義象限軸線並與使用者確認

- **Y軸（金額）**：先不用問使用者，蒐集完產品後再決定座標範圍與幣別（若品牌橫跨不同國家/幣別，統一換算成同一幣別，並在圖上註明匯率與換算日期）。若價格分布跨越一個數量級以上（例如 $40–$1000），用對數座標（log scale），避免低價品全部擠在底部。
- **X軸（風格）**：Claude 根據產品類別提出一組風格光譜建議（寫出兩端各自代表什麼設計語言/特徵），用一句話跟使用者確認：「風格光譜擬用『A vs B』，需要調整嗎？」若使用者沒有回應但明顯在無人看管狀態下執行（見系統的 unattended 判斷），就採用建議並在輸出中註明是 Claude 自行判斷的光譜。
- **背景分層門檻（入門/中階/高階）**：依 Claude 對該類別的市場常識設定價格帶（例如藍牙耳機：入門 < $50、中階 $50-150、高階 > $150），在最終輸出中寫明這個判斷依據，讓使用者可以之後手動修改。

### 3. 研究階段（逐品牌執行）

對每個品牌：
- 用 WebSearch / WebFetch，或（若有連結的瀏覽器工具可用）內建瀏覽器/Claude in Chrome，實際查詢該品牌目前在 Amazon 與官網販售的產品
- 依使用者指定的每品牌代表產品數量，選出代表性產品，蒐集：品牌名、產品名稱、售價（含幣別）、直接銷售連結（Amazon優先，其次官網，記錄是哪一種 link_type）、可用來判斷風格定位的產品描述/材質/設計特徵
- 找不到某品牌合適產品時，如實記錄「未找到」，不要編造

對每個產品，依已確認的風格光譜，評出 X 軸座標（0-100），並簡短記下評分理由（例如：「線條俐落無裝飾、金屬霧面 → 偏極簡端 85分」），方便使用者檢視這個主觀判斷。資料結構統一比照 `reference/example-products.json`：

```json
{
  "id": "brand-product",
  "brand": "品牌名",
  "product": "產品全名",
  "price": 129.99,
  "price_note": "缺貨改採近期市場參考價 等備註，沒有就留空字串",
  "tier": "entry | mid | high",
  "style": 72,
  "style_reason": "評分理由，會顯示在 tooltip 跟 Markdown 表格裡",
  "sales_link": "https://...（該產品的直接商品頁，不是搜尋頁）",
  "link_type": "Amazon | 官網 | Newegg 等"
}
```

**圖片處理（重要——先測試，不要假設能做到）**：Claude 執行環境（尤其雲端 sandbox）的網路出口通常只允許少數套件登錄檔網域，Bash 裡對 Amazon／品牌官網／圖床網域發 request 常會被 proxy 擋下（403 / tunnel connection failed）；`WebFetch` 工具本身也明確不支援回傳圖片內容。這代表「下載商品照片、轉 base64、內嵌進 Artifact 網頁」在很多環境下**做不到**，且 Artifact 的 CSP 也不允許直接外連 hotlink 圖片。

正確做法：
1. 在研究階段早期（例如查第一、二個品牌時），實際試一次圖片下載／WebFetch 圖片，確認這個環境能不能取得真實商品圖。不要假設可以或不可以。
2. **能取得**：正常下載、視需要壓縮尺寸控制檔案大小（Artifact 全部內嵌資料上限 16MB），轉 base64 data URI 內嵌進 HTML。
3. **不能取得**（目前多數環境是這種情況）：改用「品牌名稱文字 + 依價格帶上色的圓點」做成色塊（pill/chip）取代商品照片，色塊本身仍可點擊連到真實銷售頁。這是使用者已經確認可接受的替代方案，不用每次都重新問——但要在頁面 footer 用一段話清楚說明「因執行環境限制無法內嵌真實商品照片」，避免使用者誤以為是偷懶。
4. 不要編造或使用示意圖／預設圖示代替真實商品照片；沒有真圖就用文字色塊，這是唯一被使用者確認過的替代方案。

### 4. 建立主要交付物：互動象限網頁

- 動手畫圖前，先讀 `dataviz` skill 了解配色與圖表設計原則，再讀 `artifact-design` skill 做設計把關（這是持久化的 Artifact，必須過設計這關）
- 可以直接複製 `reference/quadrant-map-template.html` 當起點，比從零手刻更快更穩：
  - 繪圖區用 SVG 畫座標軸、格線、三色價格帶背景（`buildSVG()`）；資料點用絕對定位的 HTML 層（`chip-layer`）疊在 SVG 上，方便處理點擊/hover/focus/無障礙（比純 SVG 或純 Canvas 好處理互動）
  - Y 軸金額用 `logScaleY()` 依 `DOMAIN.priceMin/priceMax` 算對數座標；X 軸風格用線性 `xScale()`
  - 價格帶背景色帶依 `DOMAIN.tierBounds` 動態算，不要寫死在 SVG 座標裡
  - 色塊 hover/focus 顯示 tooltip：品牌、產品名、價格、價格帶、風格分數、評分理由
  - 資料表放在圖表下方，可依欄位排序，作為象限圖的文字版備援（無障礙／找不到色塊時的备援）
  - 淺色／深色主題都要可讀（跟隨 `artifact-design` 的規範：`:root` 定義淺色 token，`@media (prefers-color-scheme: dark)` 配 `:not([data-theme="light"])` 一組，`:root[data-theme="dark"]` 再一組）
- 頁首簡短說明：資料蒐集時間、風格光譜定義、價格分層依據，讓使用者知道這些是可調整的判斷，不是絕對事實
- 這種市場地圖屬於使用者會回來查看/更新/分享的類型，所以用 **Artifact 工具 publish**，不要只用 SendUserFile 丟一次性檔案

### 5. 匯出按鈕（使用者要求匯出時才做，做在頁面上而不是一次性檔案）

一旦使用者要任何一種匯出，優先做成頁面上常駐的下載按鈕（用 `artifact-capabilities` skill 的 **`downloads` 能力**），理由：發布的 Artifact 頁面本身會擋掉所有自發下載（`<a download>`、blob/data URL、腳本觸發存檔都是無效的），`downloads` 能力是唯一能真的把檔案存到使用者裝置的方式；而且做成按鈕之後，之後資料或圖表有調整，使用者自己按一下就能拿到最新版，不用每次都回來問 Claude 要。

發布前**務必**先讀 `artifact-capabilities` skill 確認目前 runtime contract 版本的 `downloads` API 型別定義，並在 `Artifact` 發布時帶上 `capabilities: {"downloads": true}`（省略 `capabilities` 會沿用上次宣告，清空要傳 `{}`）。實作參考 `reference/quadrant-map-template.html` 裡的 `setupDownloads()`：`await claude.use("downloads")` 拿不到就隱藏按鈕（不能假設同步可用），拿到後才把按鈕 `hidden = false`。

三種常見匯出，各自建議放的位置：

1. **象限圖 PNG**：按鈕放在圖表卡片（`.chart-card`）的 toolbar，緊鄰圖表標題。用 `html2canvas`（從 `cdnjs.cloudflare.com` 載入，釘住版本如 `1.4.1`）把整個 `.chart-card` DOM 節點（SVG + HTML 色塊層）渲染成 canvas，再 `canvas.toBlob('image/png')` 產生 Blob 丟給 `downloads.save({filename, data: blob})`。
2. **資料表 Markdown**：按鈕放在表格卡片（`.table-card`）的 toolbar。純資料匯出：品牌／產品／售價／價格帶／風格分數／評分理由／銷售頁的 Markdown 表格，`buildMarkdown()` 參考範例即可，不含版面設計資訊。
3. **Figma Make 重現規格 Markdown**：如果使用者提到想在 Figma / Figma Make 裡重現這張圖，按鈕放在圖表卡片 toolbar、緊鄰 PNG 匯出按鈕（兩者功能相關：一個是圖，一個是重建圖的規格）。內容要包含：圖表概念說明、版面配置尺寸、色彩 design tokens（明暗兩套，CSS 變數名+hex值對照表）、字體設定、座標換算公式、**每個資料點預先算好的 X%／Y% 座標**（方便在 Figma 裡直接定位，不用重算 log scale）、一段可直接貼給 Figma Make 的提示詞。這份文件要**即時從頁面目前的 `PRODUCTS`／`DOMAIN` 資料算出**（`buildFigmaSpecMarkdown()`），不要寫死成靜態字串，這樣資料或配色改了，重新按一次匯出就會自動同步，不會跟畫面內容脫節。

三顆按鈕都要：disable + 顯示「匯出中…」防止重複點擊、`catch` 時對 `err.code === 'declined'`（使用者自己按取消）不跳錯誤訊息、其他錯誤才 `alert` 並印 console。

### 6. 完成前驗證（務必做，不要跳過）

- 抽查至少3-5個銷售連結，確認真的能打開、是「直接商品頁」而不是搜尋結果頁、且對應到正確產品
- 檢查象限座標分佈是否合理（沒有大量重疊到無法辨識、沒有離譜的離群值未附註說明）
- 若有做圖片內嵌：檢查所有圖片是否成功顯示（沒有破圖/空白）；若用色塊替代：檢查文字有沒有截斷
- 檢查頁面在淺色與深色模式下都可讀（用無頭瀏覽器截兩種模式的圖比對）
- 若做了匯出按鈕：實際點一次每顆按鈕，確認 `downloads` 能力有正確回應、檔案內容跟畫面資料一致，且按鈕在常見版面寬度下不會擠在一起或換行
- 用 `node --check` 驗證抽出來的 inline `<script>` 沒有語法錯誤，再發布

## 注意事項

- 蒐集到的價格、風格評分、銷售連結都是某個時間點的快照，會在頁面上註明蒐集日期，避免使用者誤以為是即時資料
- 風格評分是 Claude 的主觀判斷，一律在頁面上說明評分邏輯，讓使用者可以要求調整
- 若使用者之後要求「換個品牌類別重做一次」「調整風格光譜重新評分」「加匯出按鈕」等後續調整，直接在同一個 Artifact 網頁上更新重新 publish（同一個 URL），不用整個重建成新網頁
